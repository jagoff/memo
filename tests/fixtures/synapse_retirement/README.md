# Synapse retirement parity fixtures

These redacted, deterministic fixture descriptors are evaluated only through
Memo-native routes.  They contain query text and expected source identifiers;
they never contain a Synapse answer, trace body, or executable code.
