# Memo native integration issue ledger — 2026-07-31

This ledger records every bug or improvement found while proving the Synapse
and Memflow absorption. Items are closed only with a reproducible check.

| ID | Severity | Status | Finding | Evidence / closure gate |
|---|---|---|---|---|
| MNI-001 | critical | resolved | Fresh hermetic `Memory(Config(...))` instances attempted productive Secure Enclave enrollment, causing 173 setup errors when the packaged helper was absent. | `MEMO_OPERATIONAL_V2_AUTO_ACTIVATE=0` isolates general tests; explicit/activated v2 still wins. `tests/test_operational_activation.py`: 7 passed; representative legacy regressions: 42 passed. |
| MNI-002 | high | resolved | The new activation switch used an unmapped `operational` TUI group and cascaded into 54 configuration failures. | Mapped to the existing `misc` domain; focused configuration run reduced to one independent catalog issue. |
| MNI-003 | high | resolved | Runtime-only authority objects (`operational_signer`, `operational_context_provider`, `operational_epoch_fence`) were Pydantic fields but absent from the configuration catalog and could enter serialization paths. | Marked runtime-only, excluded from repr/serialization/TUI. Catalog + spawned-process persistence: 9 passed. |
| MNI-004 | critical | resolved | Contradiction emission called public `operational.ledger.append`, which is correctly read-only and bypassed the authenticated Memo store path. | Added `record_anomaly`; v1 commits through the fence and v2 emits canonical signed conflict lifecycle events. Included in 168 focused passes. |
| MNI-005 | critical | resolved | Several legacy test/runtime paths constructed a v1 `OperationalStore` without the required epoch authority, so focus/conflict/receipt/sync writes failed closed. | Composed real in-memory roster, signer, epoch marker and fence; 168 focused passes without restoring unauthenticated append. |
| MNI-006 | high | resolved | `memo_signal_remember` and `memo_signal_list` were registered but missing from agent-profile declarations and exact tool-count metadata. | Runtime/profile/docs now agree on agent 49, core 66, full 170 after including the nine mesh tools; profile/surface checks pass. |
| MNI-007 | medium | resolved | README exact CLI inventory no longer matched the registered top-level command set after integration. | README now lists all 142 commands, including `chat-session`, `events`, `definitive`, and `mesh`; the exact supply-chain check passes. |
| MNI-008 | critical | resolved | `memo sync bootstrap` aborted after its canonical reindex succeeded when the optional operational receipt had no authority in a pre-activation destination. | Receipt still fails closed but cannot roll back/obscure a completed rebuild; all 8 bootstrap tests pass. |
| MNI-009 | medium | resolved | The whole-source Ruff gate reported 24 failures: merge-regressed formatting/imports plus one incorrect Ruff suppression for a constant HTTPS URL. | `uv run --no-sync ruff check src/ tests/`: zero findings. |
| MNI-010 | external gate | blocked | The reproducible A↔B proof is hermetic; physical two-Mac transport, reboot recovery, frozen signed capability manifest, production key/terminal registration, cutover and retirement remain unproven. | Requires two reachable Macs and approved production deployment/cutover authority. |
| MNI-011 | high | resolved | The local terminal-event JSONL performed check-then-append without a cross-process lock; concurrent terminals could duplicate one event id or interleave writes. | Cross-process authority lock + no-follow durable append + corruption/idempotency gates. 4 concurrency/storage tests pass. |
| MNI-012 | high | resolved | `chat-session` performed unlocked read-modify-write of one JSON file; terminal A and B could silently overwrite each other's turns. | Locked atomic mutations and payload-conflict rejection; an 8-process terminal test plus 2 storage/idempotency tests pass. |
| MNI-013 | low | resolved | The new chat-turn mapping inferred string-only values, so adding its numeric timestamp failed strict static typing. | Declared the heterogeneous mapping explicitly; full `mypy` gate required for closure. |
| MNI-014 | medium | resolved | The green non-slow suite emitted 42 warnings: unclosed SQLite connections plus Python 3.14 warnings for a deliberate test-only `fork()`. | All identified connections now close deterministically; the deliberate fork warning is locally scoped with its rationale. Warning-focused integration set: 146 passed with `ResourceWarning` and `PytestUnraisableExceptionWarning` as errors. |
| MNI-015 | medium | resolved | `connect_operational_db()` leaked its newly opened connection if a configuration PRAGMA failed before returning ownership to the caller. | Fail-safe close around post-connect setup plus injected-PRAGMA regression; `tests/test_operation_views_v2.py`: 21 passed with `ResourceWarning` as error. |
| MNI-016 | critical | resolved | `TerminalBridge` treated any non-empty terminal registration signature as authentic; the definitive proof used placeholder strings. | Domain-separated registration signatures bind terminal, principal, session, UID, capability, nonce, device, key and roster; default verifier fails closed and tamper/fake-signature tests pass. |
| MNI-017 | critical | resolved | Terminal idempotency read happened before `BEGIN IMMEDIATE`, so two processes could both reserve and execute one presenter effect. | Reservation SELECT+write is one immediate transaction; a synchronized two-process test observes one external presentation only. |
| MNI-018 | critical | resolved | The claimed independent peers shared one in-memory private-key store and one transport object/path. | A and B now own disjoint private-key stores and disjoint Git clones, exchanging only immutable signed artifacts through a bare remote. |
| MNI-019 | critical | resolved | `restart_rebuild` only reinstantiated Python objects in the same process while claiming a process restart. | A clean interpreter subprocess reopens the pinned authority and ledger, verifies every event, rebuilds a fresh view twice and proves stable deduplication/continuity with a distinct PID. |
| MNI-020 | high | resolved | `TemporaryDirectory` deleted all underlying proof artifacts; opaque hashes alone were not independently auditable. | Deterministic evidence ZIP persists both ledgers, authorities, Git artifacts, terminal receipts and a manifest; its SHA-256 is included in the signed receipt. |
| MNI-021 | critical | resolved | A self-consistent attacker-controlled receipt+roster pair verified without an external trust root. | Receipt embeds the roster hash; verification requires an independently supplied trusted roster hash and binds signer device/key. Simultaneous receipt+roster substitution is a regression test. |
| MNI-022 | critical | resolved | A peer key could sign an event whose actor claimed another device. | Append and import require `actor.device_id == origin_device` and the active origin key; impersonation is rejected before reduction. |
| MNI-023 | critical | resolved | `local_only` events were present in the same exported bundle as shared events and could leave the device. | Imports reject local-only federation; export truncates only a private tail and fails closed on an unverifiable local/shared interleave. Cross-peer services now emit explicit shared visibility. |
| MNI-024 | critical | resolved | A non-recipient identity could reserve or transition another actor's delivery, including through imported events. | API selection and reducers bind reserve/transition/ACK to target actor/principal; attack regressions cover direct and signed-import paths. |
| MNI-025 | critical | resolved | Any peer could expire another peer's presence lease. | Renew/expire API and reducers require the lease owner; foreign signed expiry is ignored/rejected by tests. |
| MNI-026 | critical | resolved | A corrupt `events/context.json` silently reset to epoch 0 and could pass an `expected_epoch=0` fence. | Missing context remains a distinct state; corrupt/unreadable/invalid context now raises a typed storage failure and never admits the event. |
| MNI-027 | critical | resolved | The signed coordination, delivery, presence, sync and terminal services were exercised only by internal Python proof code, not a productive Memo CLI/MCP surface. | `memo mesh` and nine default-profile `memo_mesh_*` tools now expose status, publish/ingest, directed message, delivery reserve/ACK, and presence. The vertical regression drives CLI A→B and MCP B→A through isolated local states and Git clones. |
| MNI-028 | medium | resolved | `memo events` and `memo chat-session` are local diagnostic stores and could be mistaken for replicated terminal coordination. | CLI help, README, and reference docs label both local-only/non-replicated and route cross-terminal use to the signed mesh. |
| MNI-029 | critical | resolved | A mesh caller could choose an `actor_id` equal to another terminal principal and potentially satisfy recipient authorization without owning that device. | Mesh actor ids reserve `:` for principals; directed recipients must use `device:session`, whose device component is derived from local configuration. Attack regressions reject both principal-shaped actor ids and bare actor-addressed mesh delivery. |
| MNI-030 | medium | resolved | Mesh MCP status/list tools were marked read-only even though constructing a remote-backed endpoint can fetch/merge its isolated Git clone. | All remote-capable mesh MCP tools use the conservative open-world `NETWORK_WRITE` annotation so clients do not silently treat network activity as a local read. |
| MNI-031 | low | resolved | Three narrative sections of the reference still advertised the pre-mesh 40/57/161 MCP counts despite exact tables and runtime metadata using 49/66/170. | All prose, tables, runtime metadata, and exact profile tests now agree on 49/66/170 and ~7k/~8.2k/~21k schema-token estimates. |
| MNI-032 | medium | resolved | The new CLI/MCP `transport_path` help said “local/shared”, which could encourage both terminals to mutate one checkout and contradict the proven isolated-clone topology. | Product help now requires an explicit isolated local clone; `--remote` is the sole documented cross-device rendezvous. |
| MNI-033 | critical | resolved | Fresh Linux CPU installs attempted macOS Keychain/Secure Enclave authority enrollment and crashed on the first `memo save`, blocking the real Ubuntu smoke gate. | Fresh non-macOS installs now retain the compatible v1 backend until a non-Keychain authority provider exists; a Linux selector regression plus release focal tests pass. |

## Empirical proof already closed

- Signed two-peer A↔B message, ACK, presence and continuity exchange: 13/13 checks.
- Exactly-once terminal presentation in both directions.
- Git gap detection/recovery, tamper rejection and process-restart rebuild.
- Separate private-key stores, separate Git clones, bare-remote rendezvous, and a
  clean interpreter reopen with a distinct PID.
- Signed receipt: `56c0ad66bd8c13febc8ad418c8a9948c2134ae3a3befcbeeea081473334ea41a`.
- Independently pinned roster: `6c010dea98b568d131f75800032b3131b5ce9242be18f4f1cd7c100b6a0cbad2`.
- Evidence archive (70 files): `0210705996291f3609af9ffebeb8d539b4f53dc708d1322715e3971bde6c471e`.
- CI-parity verification: Ruff clean, mypy clean across 513 source files,
  6,843 non-slow tests passed (20 skipped), 78.63% coverage, and all 9 slow
  tests passed.
- Physical two-Mac reboot/deployment remains the explicit external gate MNI-010.
