# Allowlist-only build context: remote builders never receive vaults, state,
# credentials, tests, backups, or unrelated developer files.
**
!pyproject.toml
!uv.lock
!README.md
!LICENSE
!server.json
!src/
!src/**
!.agents/
!.agents/**
!.claude-plugin/
!.claude-plugin/**
!commands/
!commands/**
!eval/
!eval/**
!hooks/
!hooks/**
!plugins/
!plugins/**
!skills/
!skills/**
!statusline/
!statusline/**

# Re-exclude local/runtime files below allowlisted directories. Docker uses
# the last matching pattern, so these must remain after the inclusions above.
**/.DS_Store
**/.env
**/.env.*
**/.envrc
**/__pycache__/
# Generated/ignored mirror of the canonical force-included release assets.
# Excluding it prevents duplicate wheel members in Docker build contexts.
src/memo/agent_assets/
**/*.db
**/*.py[cdo]
**/*.sqlite*
**/config.local.*
